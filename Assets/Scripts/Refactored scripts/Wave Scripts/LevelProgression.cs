using UnityEngine;
using DG.Tweening;

public class LevelProgression : MonoBehaviour
{/*
    [System.Serializable]
    public class WaveUnlock
    {
        public int waveNumber; 
        public GameObject objectToDisable;
    }

    [SerializeField] private WaveUnlock[] waveUnlocks;

    [Header("Unlock animation")]
    [SerializeField] private float unlockAnimationDuration = 1f;
    [SerializeField] private float unlockAnimationOffset = 10f;
    public void OnWaveCompleted(int waveNumber)
    {
        foreach (WaveUnlock i in waveUnlocks)
        {
            if (i.waveNumber -1 == waveNumber)
            {
                i.objectToDisable.transform.DOLocalMoveY(-unlockAnimationOffset, unlockAnimationDuration); // Lower barriers into ground
            }
        }
    }*/
}
